"""MiniCPM5-bound Agent Runtime.

Import concrete classes from ``texada.agent.protocol`` or
``texada.agent.runtime``. Keeping this package initializer light prevents the
model adapter and runtime from importing each other during startup.
"""
