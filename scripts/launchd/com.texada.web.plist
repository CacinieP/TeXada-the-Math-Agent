<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.texada.web</string>

    <key>ProgramArguments</key>
    <array>
        <!-- `cd /; cd PROJECT` breaks launchd's getcwd failure chain (the
             initial cwd is inaccessible, so a single `cd` inherits the
             failure). http.server serves from --directory regardless of cwd. -->
        <string>/bin/sh</string>
        <string>-c</string>
        <string>cd /; cd "{{PROJECT_ROOT}}"; PYTHONUNBUFFERED=1 "{{PROJECT_ROOT}}/.venv/bin/python" -u -m http.server "{{WEB_PORT}}" --bind "{{WEB_HOST}}" --directory "{{PROJECT_ROOT}}/tauri-shell/src"</string>
    </array>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
        <key>PYTHONUNBUFFERED</key>
        <string>1</string>
    </dict>

    <key>StandardOutPath</key>
    <string>{{LOG_DIR}}/web-service.log</string>

    <key>StandardErrorPath</key>
    <string>{{LOG_DIR}}/web-service.log</string>

    <key>KeepAlive</key>
    <dict>
        <key>SuccessfulExit</key>
        <false/>
        <key>Crashed</key>
        <true/>
    </dict>

    <key>RunAtLoad</key>
    <true/>

    <key>ThrottleInterval</key>
    <integer>10</integer>
</dict>
</plist>
